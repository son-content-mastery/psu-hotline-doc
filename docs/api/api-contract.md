# API Contract

This document is the human-readable source of truth for the HoTLinE Doc MVP API. The OpenAPI schema generated by the backend must describe the same behavior. If implementation and this contract disagree, update one or both in the same change.

## Conventions

- Base path: `/api/v1/`
- Transport: HTTPS outside local development.
- Payloads: JSON, except document upload uses `multipart/form-data`.
- Naming: `snake_case` in requests and responses.
- Identifiers: opaque positive integer IDs. A client must not infer access from an ID.
- Dates and times: ISO 8601; timestamps include a timezone and are returned in UTC. Date-only values use `YYYY-MM-DD`.
- Money: decimal strings plus ISO 4217 currency, for example `"amount": "10000.00", "currency": "THB"`.
- Locale: `Accept-Language: th` or `Accept-Language: en`. Unsupported or missing master-data translations fall back to Thai, then to a stable code. The response may include `translation_fallback: true`.
- User-facing application and document statuses are translated by the frontend. The API returns stable status codes and never expects the frontend to decide a transition.
- List endpoints use DRF pagination: `{ "count", "next", "previous", "results" }`.

Unless noted otherwise, a successful create returns `201 Created`, reads return `200 OK`, updates/actions return `200 OK`, and logout returns `204 No Content`.

### Error shape

Expected errors use one consistent envelope:

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Some fields are invalid.",
    "fields": {
      "rooms": ["Must be at least 1."]
    }
  }
}
```

`fields` is optional. Stable error codes are intended for frontend behavior; `message` is safe, human-readable context and is not a substitute for translated UI copy.

Common responses:

| HTTP | Code | Meaning |
| --- | --- | --- |
| `400` | `VALIDATION_ERROR` | Malformed or inconsistent input. |
| `401` | `AUTHENTICATION_REQUIRED` / `INVALID_CREDENTIALS` | No valid session, or login failed. |
| `403` | `CSRF_FAILED` / `PERMISSION_DENIED` | Request is authenticated but not permitted, or CSRF validation failed. |
| `404` | `NOT_FOUND` | Resource does not exist **or is outside the caller's object scope**. |
| `409` | `INVALID_STATUS_TRANSITION` / endpoint-specific code | Current state conflicts with the requested action. |
| `413` | `FILE_TOO_LARGE` | Upload exceeds the configured 10 MiB MVP limit. |
| `415` | `UNSUPPORTED_FILE_TYPE` | Extension, declared MIME type, or detected content is not permitted. |
| `429` | `RATE_LIMITED` | Login or another protected endpoint was throttled. |

Unexpected server errors use `INTERNAL_ERROR` without exposing stack traces, secrets, filesystem paths, or personal data.

## Authentication, sessions, and CSRF

### `GET /api/v1/system/status/`

**Purpose:** Return the currently active bilingual maintenance window for the requested locale.

**Permission:** Public. No session or CSRF cookie is required.

**Success:** `{ "maintenance": null, "checked_at": "..." }` or a `maintenance` object containing only localized `title`, localized `message`, `starts_at`, and `ends_at`.

### `GET /api/v1/public/providers/`

**Purpose:** Return active optional private-provider listings with a mandatory localized non-endorsement disclaimer. Optional `?service=` accepts only `APPLICATION_SUPPORT`, `TECHNICAL_DRAWING`, `FIRE_SAFETY`, or `LEGAL_ADVICE`.

**Permission:** Public.

**Success:** `disclaimer` plus `results` containing name, service codes, nullable reference price range/currency/localized note, optional contact/source URL, source status, and source-check date. A listing is not proof of licensing, qualification, quality, availability, or price.

The MVP uses Django's server-side session authentication. It does **not** put access tokens or passwords in `localStorage` or `sessionStorage`.

- Login creates a Django session and sets an `HttpOnly`, `SameSite=Lax` session cookie.
- `GET /api/v1/auth/me/` sets or refreshes the readable CSRF cookie used by the SPA, including for an unauthenticated visitor.
- Every mutating request sends that CSRF value as `X-CSRFToken` and includes credentials.
- Production cookies are `Secure`; local HTTP development is the documented exception.
- Logout invalidates the server-side session and expires its cookie.
- The Vite development server proxies `/api` and `/media` to Django, keeping browser requests same-origin. The frontend should use relative URLs.

Anonymous classification answers are held in Pinia and `sessionStorage` only so a page refresh or login redirect does not discard progress. The browser never supplies a trusted classification result. After authentication, `POST /applications/` sends the original answers and Django evaluates the current database rules again in the same application-creation operation.

## Permission and object-scope summary

| Area | Applicant | Local officer | Central officer | Super admin | Anonymous |
| --- | --- | --- | --- | --- | --- |
| Classification and public master data | Read/evaluate | Read/evaluate | Read/evaluate | Read/evaluate | Read/evaluate |
| Applicant applications | Own objects only | No access through applicant endpoints | No individual access | Django Admin | None |
| Officer review | None | Applications in own `LocalAuthority` only | None | Django Admin | None |
| Central summary | None | None | Aggregated province data only | Django Admin | None |
| License | Own approved application | Same-authority application when required for work | No individual access | Django Admin | None |

All scoping is applied to backend querysets before object lookup. Out-of-scope object requests return `404`, not a response that confirms the object exists. `SUPER_ADMIN` management is deliberately provided through Django Admin rather than a public SPA API.

---

## Authentication

### `POST /api/v1/auth/login/`

**Purpose:** Authenticate a demo user and start a server-side session.

**Permission:** Anonymous. An already authenticated user may log in again; the old session is rotated. Login is throttled.

**Request**

```json
{
  "email": "applicant@example.test",
  "password": "demo password supplied out of band"
}
```

**Success — `200 OK`**

```json
{
  "user": {
    "id": 1,
    "email": "applicant@example.test",
    "display_name": "ผู้ประกอบการตัวอย่าง",
    "role": "APPLICANT",
    "local_authority": null
  }
}
```

**Important errors:** `400 VALIDATION_ERROR` for missing fields; `401 INVALID_CREDENTIALS` uses the same message for an unknown account and a wrong password; `403 CSRF_FAILED`; `429 RATE_LIMITED`. A disabled user is reported as invalid credentials.

### `POST /api/v1/auth/logout/`

**Purpose:** Invalidate the current session.

**Permission:** Any authenticated role.

**Request:** Empty JSON object or no body.

```json
{}
```

**Success — `204 No Content`:** Empty body; session cookie is expired.

**Important errors:** `401 AUTHENTICATION_REQUIRED`; `403 CSRF_FAILED`.

### `GET /api/v1/auth/me/`

**Purpose:** Bootstrap SPA authentication state and set/refresh the CSRF cookie.

**Permission:** Public. This endpoint intentionally returns an explicit unauthenticated state instead of `401` so the SPA can bootstrap cleanly.

**Request:** No body.

**Success — authenticated**

```json
{
  "authenticated": true,
  "user": {
    "id": 2,
    "email": "officer.patong@example.test",
    "display_name": "เจ้าหน้าที่ตัวอย่าง ป่าตอง",
    "role": "LOCAL_OFFICER",
    "local_authority": {
      "id": 4,
      "code": "PATONG_MUNICIPALITY",
      "name": "เทศบาลเมืองป่าตอง"
    }
  }
}
```

**Success — anonymous**

```json
{
  "authenticated": false,
  "user": null
}
```

**Important errors:** None expected for ordinary bootstrap. Server failures use `500 INTERNAL_ERROR` and still must not disclose session details.

### `POST /api/v1/auth/register/`

**Purpose:** Create an applicant account and queue a time-limited email-activation link without allowing public role assignment.

**Permission:** Anonymous. CSRF-protected and throttled by source plus a hashed normalized email key.

```json
{
  "email": "applicant@example.test",
  "password": "user-supplied password",
  "password_confirmation": "user-supplied password",
  "terms_accepted": true
}
```

**Success — `202 Accepted`**

```json
{ "accepted": true }
```

The server always assigns `APPLICANT`, null authority, non-staff, and non-superuser values. It also assigns a neutral localized display name and selects the stored notification language from a supported `Accept-Language` value (`en` or Thai fallback). Unknown fields—including display name, language, role, authority, staff, superuser, active, and verification fields—are rejected. A duplicate valid request returns the same response and never overwrites the existing profile, language, or password. New accounts cannot log in until activation succeeds.

**Important errors:** `400 VALIDATION_ERROR` for field, consent, password confirmation, or password-policy failures; `403 CSRF_FAILED`; `429 RATE_LIMITED`.

### `POST /api/v1/auth/activation/resend/`

**Purpose:** Queue another activation message without revealing whether an unverified account exists.

**Permission:** Anonymous. CSRF-protected and throttled by source plus hashed normalized email.

```json
{ "email": "applicant@example.test" }
```

Every syntactically valid email receives `202 {"accepted": true}`. Only an active, unverified account creates an outbox record, and requests within the same minute share an idempotency key.

**Important errors:** `400 VALIDATION_ERROR`; `403 CSRF_FAILED`; `429 RATE_LIMITED`.

### `POST /api/v1/auth/activation/confirm/`

**Purpose:** Consume one purpose-specific signed activation token and record server-owned email verification time.

**Permission:** Anonymous. CSRF-protected and throttled.

```json
{ "token": "signed-token-from-email" }
```

**Success — `200 OK`**

```json
{ "activated": true }
```

The token expires after the configured lifetime and becomes invalid after use or if the account email changes. It is never returned by another API response or stored in the outbox.

**Important errors:** `400 ACTIVATION_INVALID`; `403 CSRF_FAILED`; `429 RATE_LIMITED`.

### `POST /api/v1/auth/password-reset/`

**Purpose:** Request a time-limited password-reset link without revealing whether an account exists.

**Permission:** Anonymous. CSRF-protected and throttled independently from login.

**Request**

```json
{ "email": "applicant@example.test" }
```

**Success — `202 Accepted`**

```json
{ "accepted": true }
```

The same response is returned for an unknown, disabled, unverified, or active verified email. For an active verified account with a usable password, Django queues an idempotent outbox message; the worker creates the single-use token at delivery time and sends a link based on configured `FRONTEND_BASE_URL`. Local demo delivery uses the console email backend; production must configure an approved mail provider.

**Important errors:** `400 VALIDATION_ERROR`; `403 CSRF_FAILED`; `429 RATE_LIMITED`.

### `POST /api/v1/auth/password-reset/confirm/`

**Purpose:** Validate a single-use reset token and set a password that passes Django's configured password validators.

**Permission:** Anonymous. CSRF-protected and throttled.

```json
{
  "uid": "encoded-user-id",
  "token": "single-use-token",
  "new_password": "new password supplied by the user"
}
```

**Success — `200 OK`**

```json
{ "reset": true }
```

**Important errors:** `400 PASSWORD_RESET_INVALID` for an expired, malformed, or reused token; `400 VALIDATION_ERROR` for password-policy failures; `403 CSRF_FAILED`; `429 RATE_LIMITED`.

---

## Classification

### `GET /api/v1/classification/questions/`

**Purpose:** Return the ordered, database-backed questions needed by the MVP wizard.

**Permission:** Public.

**Request:** No body; locale comes from `Accept-Language`.

**Success**

```json
{
  "version": "2026-09-19T04:00:00Z",
  "questions": [
    {
      "key": "rooms",
      "order": 1,
      "type": "integer",
      "label": "ที่พักของคุณมีห้องพักทั้งหมดกี่ห้อง?",
      "required": true,
      "minimum": 1
    },
    {
      "key": "guests",
      "order": 2,
      "type": "integer",
      "label": "รองรับผู้เข้าพักได้สูงสุดกี่คน?",
      "required": true,
      "minimum": 1
    },
    {
      "key": "has_restaurant",
      "order": 3,
      "type": "boolean",
      "label": "มีห้องอาหารหรือไม่?",
      "required": true
    }
  ]
}
```

**Important errors:** `400 UNSUPPORTED_LOCALE` only if strict locale validation is later enabled; for MVP, unsupported locales fall back as described above.

### `POST /api/v1/classification/evaluate/`

**Purpose:** Evaluate answers against active database rules and return guidance before login.

**Permission:** Public. Rate limiting may be applied without changing result semantics.

**Request**

```json
{
  "rooms": 20,
  "guests": 40,
  "has_restaurant": false
}
```

**Success**

```json
{
  "outcome": "TYPE_1",
  "requires_license": true,
  "property_type": {
    "id": 2,
    "code": "TYPE_1",
    "name": "ที่พักแรมประเภทที่ 1"
  },
  "fee": {
    "amount": "10000.00",
    "currency": "THB",
    "validity_years": 5
  },
  "needs_manual_classification_confirmation": false,
  "rules_version": "2026-09-19T04:00:00Z"
}
```

For `8` rooms and `36` guests, the result is `REQUIRES_LICENSE_REVIEW`, `requires_license` is `true`, `property_type` and `fee` are `null`, and `needs_manual_classification_confirmation` is `true`. No category is guessed. For more than 49 rooms, the result is `OUT_OF_SCOPE`.

For `6` rooms and `24` guests, the outcome is `NOT_HOTEL`, `requires_license` is `false`, `property_type.code` is `NON_HOTEL_NOTIFICATION`, `property_type.issues_license` is `false`, and `fee` is `null`. This enables the supplied 17-item notification checklist without describing the result as a hotel licence.

**Important errors:** `400 VALIDATION_ERROR` for missing answers, non-integers, values below 1, or non-boolean restaurant input; `409 NO_ACTIVE_CLASSIFICATION_RULE` if master data is incomplete or no unambiguous active rule matches. Overlapping active rules are a server configuration fault and must fail safely rather than picking one silently.

---

## Public master data

### `GET /api/v1/locations/phuket/`

**Purpose:** Return the pinned, database-backed Phuket province/district/subdistrict hierarchy used by applicant address controls. The response is localized with `Accept-Language`; codes remain stable.

**Permission:** Public, read-only.

```json
{
  "province": {"code": "83", "name": "ภูเก็ต"},
  "districts": [
    {
      "code": "8302",
      "name": "กะทู้",
      "subdistricts": [
        {"code": "830202", "name": "ป่าตอง", "postal_code": "83150"}
      ]
    }
  ]
}
```

The catalog is imported from the pinned Thailand Geography JSON snapshot documented with the backend data file. Clients submit only `subdistrict_code`; Django derives the province, district, subdistrict, and postal-code snapshots. This administrative hierarchy does not determine `LocalAuthority` routing.

### `GET /api/v1/local-authorities/`

**Purpose:** List the 19 Phuket authorities used to assign an application and show external-document guidance.

**Permission:** Public, read-only.

**Request example:** `GET /api/v1/local-authorities/?search=ป่าตอง`

**Success**

```json
{
  "count": 1,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 4,
      "code": "PATONG_MUNICIPALITY",
      "name": "เทศบาลเมืองป่าตอง",
      "contact_phone": "076-000-004",
      "contact_email": "demo-patong@example.test",
      "translation_fallback": false
    }
  ]
}
```

**Important errors:** `400 VALIDATION_ERROR` for invalid pagination. Contact values in seed data are explicitly mock data.

### `GET /api/v1/property-types/`

**Purpose:** Return active property types and current fee guidance for display; this endpoint does not perform classification.

**Permission:** Public, read-only.

**Request example:** `GET /api/v1/property-types/`

**Success**

```json
{
  "count": 2,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 2,
      "code": "TYPE_1",
      "name": "ที่พักแรมประเภทที่ 1",
      "description": "ข้อมูลตัวอย่างสำหรับการสาธิต",
      "current_fee": {
        "amount": "10000.00",
        "currency": "THB",
        "validity_years": 5,
        "effective_from": "2026-01-01"
      },
      "translation_fallback": false
    }
  ]
}
```

**Important errors:** `400 VALIDATION_ERROR` for invalid filters or pagination. Missing current fee data is returned as `current_fee: null`, not fabricated.

### `GET /api/v1/property-types/{id}/requirements/`

**Purpose:** Show the database-driven document checklist and external-agency guidance before authentication. This is the public checklist used between classification and login.

**Permission:** Public, read-only.

**Request example:** `GET /api/v1/property-types/2/requirements/` with `Accept-Language: en`

**Success**

```json
{
  "property_type": { "id": 2, "code": "TYPE_1", "name": "Accommodation Type 1" },
  "is_legally_validated_checklist": false,
  "disclaimer": "Demo document checklist only; confirm requirements with the responsible authority before real use.",
  "groups": [
    {
      "category": "OPERATOR_PREPARED",
      "items": [
        {
          "document_type": {
            "id": 1,
            "code": "APPLICATION_FORM",
            "name": "Application form",
            "description": "Demo application form for accommodation details",
            "instructions": "Complete and sign before uploading",
            "category": "OPERATOR_PREPARED",
            "allows_multiple_files": false,
            "translation_fallback": false
          },
          "required": true,
          "description": "Demo application form for accommodation details",
          "instructions": "Complete and sign before uploading",
          "guidance": null
        }
      ]
    }
  ],
  "steps": [
    {
      "code": "APPLICANT",
      "order": 1,
      "required": 5,
      "completed": 0,
      "action_required": 5,
      "complete": false,
      "items": []
    }
  ]
}
```

**Important errors:** `404 NOT_FOUND` for an unknown or inactive property type. The response always identifies the checklist as demo/non-authoritative; it contains no applicant data or upload status.

---

## Applicant applications

### `POST /api/v1/applications/`

**Purpose:** Create an applicant-owned draft from retained wizard answers. Django re-evaluates classification rules and derives the requirement set; the client cannot submit a classification code or fee as authoritative data.

**Permission:** `APPLICANT` only.

**Request**

```json
{
  "property": {
    "name": "ที่พักตัวอย่างอันดามัน",
    "address_line": "99 ถนนตัวอย่าง",
    "subdistrict_code": "830202",
    "local_authority_id": 4
  },
  "classification_answers": {
    "rooms": 20,
    "guests": 40,
    "has_restaurant": false
  }
}
```

**Success — `201 Created`**

```json
{
  "id": 101,
  "reference_number": null,
  "status": "DRAFT",
  "property": {
    "id": 81,
    "name": "ที่พักตัวอย่างอันดามัน",
    "local_authority": {
      "id": 4,
      "code": "PATONG_MUNICIPALITY",
      "name": "เทศบาลเมืองป่าตอง"
    }
  },
  "classification": {
    "outcome": "TYPE_1",
    "property_type": { "id": 2, "code": "TYPE_1", "name": "ที่พักแรมประเภทที่ 1" },
    "answers": { "rooms": 20, "guests": 40, "has_restaurant": false }
  },
  "requirements_complete": false,
  "created_at": "2026-09-19T04:15:00Z",
  "updated_at": "2026-09-19T04:15:00Z"
}
```

**Important errors:** `401 AUTHENTICATION_REQUIRED`; `403 PERMISSION_DENIED` for other roles; `400 VALIDATION_ERROR` for property/answer validation, an unknown/inactive administrative subdistrict, or an unknown/inactive authority; `409 NO_ACTIVE_CLASSIFICATION_RULE`; `409 OUT_OF_SCOPE` when classification is outside this MVP. Province, district, subdistrict name, and postal code are derived server-side and cannot be overridden by the client. A `REQUIRES_LICENSE_REVIEW` draft may be saved but cannot be submitted until an authorized workflow resolves its property type.

### `GET /api/v1/applications/`

**Purpose:** List the authenticated applicant's applications for tracking.

**Permission:** `APPLICANT`; results are always owner-scoped.

**Request example:** `GET /api/v1/applications/?view=action&page=1`. `view` accepts `action`, `in_progress`, `completed`, or `all`; the exact `status` filter remains available for API clients. The action view also includes an approved hotel licence whose expiry is within the configured reminder window or has passed.

**Success**

```json
{
  "count": 1,
  "next": null,
  "previous": null,
  "summary": { "needs_action": 1, "in_progress": 2, "approved": 3, "total": 7 },
  "results": [
    {
      "id": 101,
      "reference_number": "HTL-2026-00001",
      "property_name": "ที่พักตัวอย่างอันดามัน",
      "property_type": { "id": 2, "code": "TYPE_1", "name": "ที่พักแรมประเภทที่ 1" },
      "responsible_authority": {
        "id": 4,
        "code": "PATONG_MUNICIPALITY",
        "name": "เทศบาลเมืองป่าตอง"
      },
      "status": "REVISION_REQUIRED",
      "current_stage": "APPLICANT_ACTION",
      "applicant_action_required": true,
      "waiting_since": "2026-09-19T06:30:00Z",
      "requirements": {
        "required": 5,
        "approved": 3,
        "current_uploads": 5,
        "complete_for_submission": false
      },
      "renewal": null,
      "updated_at": "2026-09-19T06:30:00Z"
    }
  ]
}
```

For an approved hotel licence the item also contains `renewal` with `expires_at`, signed integer `days_remaining`, `status` (`UPCOMING`, `DUE`, or `EXPIRED`), and `action_required`. A negative day count denotes an expired licence. Other applications and non-hotel notification acknowledgements return `null`.

**Important errors:** `401 AUTHENTICATION_REQUIRED`; `403 PERMISSION_DENIED`; `400 VALIDATION_ERROR` for an unknown filter/status. Another applicant's records can never appear, including through filtering.

### `GET /api/v1/applications/{id}/`

**Purpose:** Return one applicant application with property, classification, completeness, authority, and current-action context.

**Permission:** Owning `APPLICANT` only.

**Request example:** `GET /api/v1/applications/101/`

**Success**

```json
{
  "id": 101,
  "reference_number": "HTL-2026-00001",
  "status": "UNDER_REVIEW",
  "current_stage": "LOCAL_OFFICER_REVIEW",
  "applicant_action_required": false,
  "waiting_since": "2026-09-19T05:00:00Z",
  "property": {
    "id": 81,
    "name": "ที่พักตัวอย่างอันดามัน",
    "address_line": "99 ถนนตัวอย่าง",
    "subdistrict": "ป่าตอง",
    "district": "กะทู้",
    "province": "ภูเก็ต",
    "postal_code": "83150",
    "local_authority": { "id": 4, "code": "PATONG_MUNICIPALITY", "name": "เทศบาลเมืองป่าตอง" }
  },
  "classification": {
    "outcome": "TYPE_1",
    "property_type": { "id": 2, "code": "TYPE_1", "name": "ที่พักแรมประเภทที่ 1" },
    "answers": { "rooms": 20, "guests": 40, "has_restaurant": false }
  },
  "requirements": { "required": 4, "approved": 0, "current_uploads": 4, "complete_for_submission": true },
  "responsible_authority": { "id": 4, "code": "PATONG_MUNICIPALITY", "name": "เทศบาลเมืองป่าตอง" },
  "submitted_at": "2026-09-19T05:00:00Z",
  "updated_at": "2026-09-19T05:01:00Z"
}
```

**Important errors:** `401 AUTHENTICATION_REQUIRED`; `403 PERMISSION_DENIED`; `404 NOT_FOUND` for a missing or non-owned application.

### `PATCH /api/v1/applications/{id}/`

**Purpose:** Update editable property fields or corrected classification answers. The server re-evaluates classification and requirements when relevant answers change.

**Permission:** Owning `APPLICANT`; only in `DRAFT` or when specifically editable during `REVISION_REQUIRED`. Status, owner, reference number, reviews, fees, and audit fields are read-only.

**Request**

```json
{
  "property": {
    "name": "ที่พักตัวอย่างอันดามัน (ปรับปรุง)",
    "postal_code": "83150"
  }
}
```

**Success**

```json
{
  "id": 101,
  "status": "DRAFT",
  "property": {
    "id": 81,
    "name": "ที่พักตัวอย่างอันดามัน (ปรับปรุง)",
    "postal_code": "83150",
    "local_authority": { "id": 4, "code": "PATONG_MUNICIPALITY", "name": "เทศบาลเมืองป่าตอง" }
  },
  "requirements_complete": false,
  "updated_at": "2026-09-19T04:25:00Z"
}
```

**Important errors:** `400 VALIDATION_ERROR` for invalid or read-only fields; `404 NOT_FOUND` for missing/non-owned application; `409 APPLICATION_NOT_EDITABLE` outside permitted states; `409 OUT_OF_SCOPE` or `NO_ACTIVE_CLASSIFICATION_RULE` after answer changes.

### `GET /api/v1/applications/{id}/requirements/`

**Purpose:** Return the database-derived checklist, split by operator-prepared and external documents, with current upload/review status and guidance.

**Permission:** Owning `APPLICANT` only.

**Request example:** `GET /api/v1/applications/101/requirements/`

**Success**

```json
{
  "application_id": 101,
  "is_legally_validated_checklist": false,
  "disclaimer": "รายการนี้จัดทำจากบัญชีเอกสารที่ได้รับสำหรับโครงการ และยังต้องยืนยันกับหน่วยงานที่รับผิดชอบก่อนใช้งานจริง",
  "complete_for_submission": false,
  "groups": [
    {
      "category": "OPERATOR_PREPARED",
      "items": [
        {
          "requirement_id": 21,
          "document_type": {
            "id": 1,
            "code": "APPLICATION_FORM",
            "name": "แบบคำขอ",
            "description": "แบบคำขอสำหรับกรอกข้อมูลที่พัก",
            "instructions": "กรอกข้อมูลให้ครบและลงนามก่อนอัปโหลด",
            "category": "OPERATOR_PREPARED",
            "translation_fallback": false
          },
          "required": true,
          "step_code": "APPLICANT",
          "status": "MISSING",
          "current_document_id": null,
          "current_document_ids": [],
          "latest_review_reason": null,
          "guidance": null
        }
      ]
    },
    {
      "category": "EXTERNAL_AGENCY",
      "items": [
        {
          "requirement_id": 24,
          "document_type": {
            "id": 4,
            "code": "BUILDING_PERMIT_O1",
            "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)",
            "description": "เอกสารจากหน่วยงานภายนอก",
            "instructions": "ตรวจสอบฉบับปัจจุบันกับหน่วยงานผู้ออก",
            "category": "EXTERNAL_AGENCY",
            "translation_fallback": false
          },
          "required": true,
          "status": "UPLOADED",
          "current_document_id": 990,
          "latest_review_reason": null,
          "guidance": {
            "issuing_agency": { "id": 7, "code": "DEMO_BUILDING_OFFICE", "name": "หน่วยงานอาคารตัวอย่าง" },
            "responsible_local_authority": { "id": 4, "name": "เทศบาลเมืองป่าตอง" },
            "contact_phone": "076-000-004",
            "contact_email": "demo-building@example.test",
            "source_url": "https://example.test/demo-building-office",
            "instructions": "ติดต่อหน่วยงานอาคารตัวอย่างพร้อมแบบอาคาร และยืนยันขั้นตอนจริงกับหน่วยงานผู้รับผิดชอบ",
            "required_supporting_items": ["เอกสารสิทธิหรือหนังสือยินยอมใช้ที่ดิน", "แบบแปลนอาคารสำหรับประกอบคำขอ"],
            "approximate_processing_days": 15,
            "translation_fallback": false
          }
        }
      ]
    }
  ],
  "steps": [
    {
      "code": "APPLICANT",
      "order": 1,
      "required": 5,
      "completed": 2,
      "action_required": 3,
      "complete": false,
      "items": []
    }
  ]
}
```

**Important errors:** `404 NOT_FOUND`; `409 CLASSIFICATION_UNRESOLVED` when no property type can yet determine requirements; `409 REQUIREMENTS_NOT_CONFIGURED` when required master data is missing. The API must never imply that the supplied project checklist has already received official legal validation.

### `POST /api/v1/applications/{id}/submit/`

**Purpose:** Atomically validate readiness and submit or resubmit. A first submission assigns a backend-generated reference and moves `DRAFT`/`READY_TO_SUBMIT` to `SUBMITTED`. After an officer-requested revision, the same action preserves the existing reference and moves `REVISION_REQUIRED` to `RESUBMITTED`. Both paths create status-history and audit entries.

**Permission:** Owning `APPLICANT` only.

**Request**

```json
{
  "confirm_information_is_correct": true
}
```

**Success**

```json
{
  "id": 101,
  "reference_number": "HTL-2026-00001",
  "status": "SUBMITTED",
  "current_stage": "LOCAL_OFFICER_REVIEW",
  "submitted_at": "2026-09-19T05:00:00Z"
}
```

For a revision response, `reference_number` is unchanged, `status` is `RESUBMITTED`, and `submitted_at` remains the original first-submission time; the response additionally returns the new `resubmitted_at`. All requested replacement documents must have a newer current `UPLOADED` version before this path succeeds.

**Important errors:** `400 VALIDATION_ERROR` when confirmation is absent; `404 NOT_FOUND`; `409 MISSING_REQUIRED_DOCUMENTS` with `fields.missing_document_type_ids`; `409 REVISION_ITEMS_INCOMPLETE` when a requested replacement is still missing; `409 CLASSIFICATION_UNRESOLVED`; `409 INVALID_STATUS_TRANSITION`; `409 REQUIREMENTS_CHANGED` when active requirements changed and the checklist must be refreshed. Concurrent calls must produce one reference and one transition only.

### `GET /api/v1/applications/{id}/history/`

**Purpose:** Return applicant-safe status history for the vertical tracking timeline.

**Permission:** Owning `APPLICANT` only. Internal-only audit actions are excluded.

**Request example:** `GET /api/v1/applications/101/history/`

**Success**

```json
{
  "application_id": 101,
  "current_status": "REVISION_REQUIRED",
  "waiting_since": "2026-09-19T06:30:00Z",
  "applicant_action_required": true,
  "events": [
    {
      "id": 301,
      "from_status": "DRAFT",
      "to_status": "SUBMITTED",
      "occurred_at": "2026-09-19T05:00:00Z",
      "reason": null,
      "actor_category": "APPLICANT",
      "document_type": null,
      "affected_documents": []
    },
    {
      "id": 302,
      "from_status": "UNDER_REVIEW",
      "to_status": "REVISION_REQUIRED",
      "occurred_at": "2026-09-19T06:30:00Z",
      "reason": "กรุณาอัปโหลดเอกสารที่อ่านได้ชัดเจน",
      "actor_category": "LOCAL_OFFICER",
      "document_type": { "id": 4, "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)" },
      "affected_documents": [
        { "id": 4, "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)" }
      ]
    }
  ]
}
```

**Important errors:** `404 NOT_FOUND`. `actor_category` contains only a role category (or `SYSTEM`), never an actor identity. `document_type` is populated when exactly one revision document is associated; `affected_documents` supports one or more. Actor email and other internal/PII fields are not returned to applicants.

---

## Applicant documents

### `POST /api/v1/applications/{id}/documents/`

**Purpose:** Upload the first or a replacement version of one checklist item. A type may accept either one file or an evidence bundle of up to ten files. A replacement creates a new immutable bundle version; it never overwrites the previous files.

**Permission:** Owning `APPLICANT`; application must be in an uploadable state (`DRAFT`, `READY_TO_SUBMIT`, or `REVISION_REQUIRED` for the requested item).

**Request — `multipart/form-data`**

```text
document_type_id=4
file=@building-permit.pdf
```

For a master-data type with `allows_multiple_files: true`, repeat the `files` field:

```text
document_type_id=8
files=@parking-front.jpg
files=@parking-access.jpg
```

The allowed formats are PDF, JPG/JPEG, and PNG. The maximum size is 10 MiB. Extension, declared MIME type, detected signature, and safe decoding checks must agree.

**Success — `201 Created`**

```json
{
  "id": 991,
  "application_id": 101,
  "document_type": { "id": 4, "code": "BUILDING_PERMIT_O1", "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)" },
  "version": 2,
  "attachment_index": 1,
  "status": "UPLOADED",
  "is_current": true,
  "original_filename": "building-permit.pdf",
  "content_type": "application/pdf",
  "size_bytes": 248013,
  "uploaded_at": "2026-09-19T07:15:00Z",
  "preflight": {
    "status": "LIMITED",
    "issue_codes": ["PDF_VISUAL_CHECK_UNAVAILABLE"],
    "analyzer_version": "quality-v1",
    "type_check_status": "MATCH",
    "detected_family": "BUILDING_RECORD",
    "type_analyzer_version": "ocr-family-v1",
    "analyzed_at": "2026-09-19T07:15:00Z"
  },
  "bundle_count": 1,
  "bundle_documents": [
    {
      "id": 990,
      "application_id": 101,
      "document_type": { "id": 4, "code": "SITE_PHOTOS", "name": "ภาพถ่ายสถานที่" },
      "version": 2,
      "attachment_index": 1,
      "status": "UPLOADED",
      "is_current": true,
      "original_filename": "front.jpg"
    }
  ]
}
```

**Important errors:** `400 VALIDATION_ERROR` or `MULTIPLE_FILES_NOT_ALLOWED`; `404 NOT_FOUND` for application or a document type outside the captured checklist; `409 APPLICATION_NOT_EDITABLE`; `409 DOCUMENT_NOT_OPEN_FOR_REVISION` when replacing an item not requested in a revision; `413 FILE_TOO_LARGE`; `415 UNSUPPORTED_FILE_TYPE`. Failed validation stores no file/database row. All files are validated before storage becomes current, and a successful bundle-version increment/current switch is atomic.

`preflight` is an advisory result tied to this exact file version. Image codes may include `LOW_RESOLUTION`, `TOO_DARK`, `TOO_BRIGHT`, `LOW_CONTRAST`, and `POSSIBLY_BLURRY`. A valid PDF quality result remains `LIMITED` with `PDF_VISUAL_CHECK_UNAVAILABLE`; S1B may still extract embedded text or render the first scanned page for its separate family check. `type_check_status` is `MATCH`, `POSSIBLE_MISMATCH`, `INCONCLUSIVE`, `UNAVAILABLE`, `NOT_APPLICABLE`, or `NOT_RUN`; `detected_family` is a controlled code or null. `PASS` and `MATCH` mean only that these analyzers found no warning/inconsistency. They do not establish exact document type, authenticity, legal sufficiency, or officer approval. The result never changes submission eligibility, and raw OCR text is never returned or stored.

### `GET /api/v1/applications/{id}/documents/`

**Purpose:** List current documents by default, with opt-in version history for the owner.

**Permission:** Owning `APPLICANT` only.

**Request example:** `GET /api/v1/applications/101/documents/?include_versions=true`

**Success**

```json
{
  "count": 2,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 991,
      "document_type": { "id": 4, "code": "BUILDING_PERMIT_O1", "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)" },
      "version": 2,
      "status": "UPLOADED",
      "is_current": true,
      "original_filename": "building-permit.pdf",
      "uploaded_at": "2026-09-19T07:15:00Z",
      "latest_review_reason": null,
      "preflight": {
        "status": "LIMITED",
        "issue_codes": ["PDF_VISUAL_CHECK_UNAVAILABLE"],
        "analyzer_version": "quality-v1",
        "type_check_status": "MATCH",
        "detected_family": "BUILDING_RECORD",
        "type_analyzer_version": "ocr-family-v1",
        "analyzed_at": "2026-09-19T07:15:00Z"
      },
      "download_url": "/api/v1/applications/101/documents/991/file/"
    },
    {
      "id": 990,
      "document_type": { "id": 4, "code": "BUILDING_PERMIT_O1", "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)" },
      "version": 1,
      "status": "REVISION_REQUIRED",
      "is_current": false,
      "original_filename": "old-building-permit.pdf",
      "uploaded_at": "2026-09-19T04:50:00Z",
      "latest_review_reason": "ภาพไม่ชัดเจน",
      "download_url": "/api/v1/applications/101/documents/990/file/"
    }
  ]
}
```

**Important errors:** `400 VALIDATION_ERROR` for invalid query values; `404 NOT_FOUND`. Any file-download route generated here must repeat the same ownership check and return safe content headers; media paths are never treated as public authorization.

### Protected document file routes

`GET /api/v1/applications/{id}/documents/{document_id}/file/` returns a validated file only to the owning `APPLICANT`, after checking both the parent application and nested document relationship. `GET /api/v1/officer/documents/{id}/file/` returns it only to a `LOCAL_OFFICER` assigned to the application's snapshotted responsible authority. Successful responses stream the file with its validated content type and safe `Content-Disposition`; errors are `401 AUTHENTICATION_REQUIRED`, `403 PERMISSION_DENIED` for the wrong role, or neutral `404 NOT_FOUND` for ownership/authority/nesting mismatches. Storage paths and `/media/` URLs are never authorization tokens.

---

## Local officer

### `GET /api/v1/officer/applications/`

**Purpose:** List the local officer's work queue.

**Permission:** `LOCAL_OFFICER`; queryset is always constrained to the user's `LocalAuthority`.

**Request example:** `GET /api/v1/officer/applications/?status=SUBMITTED,RESUBMITTED&property_type=TYPE_1&ordering=submitted_at`

`property_type` accepts an active stable property-type code or `UNCONFIRMED`. `ordering` accepts `submitted_at`, `-submitted_at`, `updated_at`, `-updated_at`, `property_type`, or `-property_type`. Filters only narrow the authenticated officer's authority-scoped queryset.

**Success**

```json
{
  "count": 1,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 101,
      "reference_number": "HTL-2026-00001",
      "property_name": "ที่พักตัวอย่างอันดามัน",
      "property_type": { "code": "TYPE_1", "name": "ที่พักแรมประเภทที่ 1" },
      "status": "SUBMITTED",
      "submitted_at": "2026-09-19T05:00:00Z",
      "resubmitted_at": null,
      "waiting_since": "2026-09-19T05:00:00Z",
      "documents_pending_review": 4
    }
  ]
}
```

**Important errors:** `401 AUTHENTICATION_REQUIRED`; `403 PERMISSION_DENIED`, including an officer account without an assigned authority; `400 VALIDATION_ERROR` for invalid filters. Filters never broaden authority scope.

### `GET /api/v1/officer/case-library/`

**Purpose:** Search de-identified completed cases and bilingual review FAQs as advisory reference material.

**Permission:** Verified `LOCAL_OFFICER`. The result pool may include completed cases province-wide because the response is a strict de-identified projection; applicants and central officers receive `403`.

**Query:** `q` (maximum 100 characters), `decision=APPROVED|REJECTED`, `property_type={active code}`, and standard `page`. Keyword search covers type code/name, classification outcome, decision, exact room/guest count, restaurant terms, and FAQ text/keywords.

**Success:** Standard paginated fields plus `faqs`. Each result contains only a salted `case_reference`, decision, localized property type, structured room/guest/restaurant snapshot, revision-round count, processing days, decision date, and aggregate required/approved/reviewed-document counts. FAQs contain `slug`, localized `question`/`answer`, and `updated_at`.

The response never includes source database/application/property IDs, applicant, property name, address, authority, application reference, filenames, extracted document content, or free-text review/decision reasons. There is no endpoint from a case pseudonym back to the source application.

**Important errors:** `400 VALIDATION_ERROR` for an overlong query or unknown decision/property type; `401 AUTHENTICATION_REQUIRED`; `403 PERMISSION_DENIED`.

### `GET /api/v1/officer/applications/{id}/`

**Purpose:** Return the application, current and prior document versions, reviews, and status context needed for local review.

**Permission:** `LOCAL_OFFICER` assigned to the application's exact `LocalAuthority`.

**Request example:** `GET /api/v1/officer/applications/101/`

**Success**

```json
{
  "id": 101,
  "reference_number": "HTL-2026-00001",
  "status": "UNDER_REVIEW",
  "waiting_since": "2026-09-19T06:20:00Z",
  "submitted_at": "2026-09-19T05:00:00Z",
  "resubmitted_at": null,
  "property": {
    "name": "ที่พักตัวอย่างอันดามัน",
    "local_authority": { "id": 4, "code": "PATONG_MUNICIPALITY", "name": "เทศบาลเมืองป่าตอง" }
  },
  "classification": { "outcome": "TYPE_1", "answers": { "rooms": 20, "guests": 40, "has_restaurant": false } },
  "documents": [
    {
      "id": 991,
      "document_type": { "id": 4, "code": "BUILDING_PERMIT_O1", "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)" },
      "version": 2,
      "status": "UPLOADED",
      "is_current": true,
      "version_label": "CURRENT",
      "category": "EXTERNAL_AGENCY",
      "original_filename": "building-permit-v2.pdf",
      "uploaded_at": "2026-09-19T07:15:00Z",
      "uploaded_by": { "id": 1, "display_name": "ผู้ประกอบการตัวอย่าง", "role": "APPLICANT" },
      "uploader_role": "APPLICANT",
      "download_url": "/api/v1/officer/documents/991/file/",
      "reviewable": true,
      "reviews": [],
      "versions": [
        {
          "id": 990,
          "document_type": { "id": 4, "code": "BUILDING_PERMIT_O1", "name": "ใบอนุญาตก่อสร้างอาคาร (อ.1)" },
          "version": 1,
          "status": "REVISION_REQUIRED",
          "is_current": false,
          "version_label": "PRIOR",
          "category": "EXTERNAL_AGENCY",
          "original_filename": "building-permit-v1.pdf",
          "uploaded_at": "2026-09-19T04:50:00Z",
          "uploaded_by": { "id": 1, "display_name": "ผู้ประกอบการตัวอย่าง", "role": "APPLICANT" },
          "download_url": "/api/v1/officer/documents/990/file/",
          "reviewable": false,
          "reviews": []
        }
      ]
    }
  ],
  "all_required_documents_approved": false,
  "allowed_actions": ["REVIEW_DOCUMENTS", "REJECT"]
}
```

Current documents are top-level items; older versions are nested under `versions`, clearly marked `PRIOR`, and never reviewable. `allowed_actions` is server-derived UI guidance (`REVIEW_DOCUMENTS`, `REQUEST_REVISION`, `APPROVE`, `REJECT`); each mutation still revalidates authorization and workflow state transactionally.

**Important errors:** `404 NOT_FOUND` for missing or another-authority application; `403 PERMISSION_DENIED` for the wrong role. File routes repeat authority scoping.

### `POST /api/v1/officer/documents/{id}/review/`

**Purpose:** Record an immutable review outcome for the current document version.

**Permission:** `LOCAL_OFFICER` whose authority owns the associated application.

**Request**

```json
{
  "outcome": "REVISION_REQUIRED",
  "reason": "ภาพเอกสารไม่ชัดเจน กรุณาอัปโหลดใหม่"
}
```

Allowed outcomes are `APPROVED`, `REVISION_REQUIRED`, and `REJECTED`. A non-blank human-readable reason is mandatory for the latter two and optional for approval.

The first successful document-review action on a `SUBMITTED` or `RESUBMITTED` application also moves the application to `UNDER_REVIEW`, with its own history/audit transition, in the same transaction. Merely reading the officer detail never changes status.

**Success — `201 Created`**

```json
{
  "id": 1201,
  "application_document_id": 991,
  "outcome": "REVISION_REQUIRED",
  "reason": "ภาพเอกสารไม่ชัดเจน กรุณาอัปโหลดใหม่",
  "reviewed_at": "2026-09-19T06:20:00Z",
  "reviewed_by": { "id": 2, "display_name": "เจ้าหน้าที่ตัวอย่าง ป่าตอง" }
}
```

**Important errors:** `400 REASON_REQUIRED` or invalid outcome; `404 NOT_FOUND` for document/authority mismatch; `409 DOCUMENT_VERSION_NOT_CURRENT`; `409 APPLICATION_NOT_REVIEWABLE`; `409 DOCUMENT_ALREADY_REVIEWED`. The client cannot set reviewer or timestamps.

### `POST /api/v1/officer/applications/{id}/approve/`

**Purpose:** Approve an application atomically, create its immutable audit/history entries, and create exactly one decision artifact. Hotel types create a licence from the effective fee schedule and fee snapshot; `NON_HOTEL_NOTIFICATION` creates a fee-free notification acknowledgement.

**Permission:** Same-authority `LOCAL_OFFICER`.

**Request**

```json
{
  "note": "ตรวจสอบเอกสารครบถ้วน"
}
```

**Success**

```json
{
  "id": 101,
  "status": "APPROVED",
  "approved_at": "2026-09-19T08:00:00Z",
  "license": {
    "id": 501,
    "artifact_kind": "HOTEL_LICENSE",
    "license_number": "LIC-2026-00001",
    "issued_at": "2026-09-19T08:00:00Z",
    "expires_at": "2031-09-18",
    "property_type": { "code": "TYPE_1", "name": "ที่พักแรมประเภทที่ 1" },
    "fee_amount_snapshot": "10000.00",
    "currency": "THB"
  }
}
```

**Important errors:** `404 NOT_FOUND`; `409 INVALID_STATUS_TRANSITION`; `409 DOCUMENTS_NOT_APPROVED` with pending IDs; `409 FEE_SCHEDULE_NOT_FOUND`; `409 LICENSE_ALREADY_EXISTS`. Retries must not create duplicate licenses or audit entries.

For a non-hotel notification, `artifact_kind` is `NOTIFICATION_ACKNOWLEDGEMENT`, the number starts with `ACK-`, and `expires_at`, `fee_amount_snapshot`, and `currency` are null. No fee schedule is required.

### `POST /api/v1/officer/applications/{id}/request-revision/`

**Purpose:** Move a reviewable application to `REVISION_REQUIRED` after one or more current documents have been marked for revision, creating history and audit entries.

**Permission:** Same-authority `LOCAL_OFFICER`.

**Request**

```json
{
  "reason": "มีเอกสารที่ต้องแก้ไข 1 รายการ"
}
```

**Success**

```json
{
  "id": 101,
  "status": "REVISION_REQUIRED",
  "applicant_action_required": true,
  "reason": "มีเอกสารที่ต้องแก้ไข 1 รายการ",
  "changed_at": "2026-09-19T06:30:00Z"
}
```

**Important errors:** `400 REASON_REQUIRED`; `404 NOT_FOUND`; `409 INVALID_STATUS_TRANSITION`; `409 NO_DOCUMENT_REVISIONS_REQUESTED`. Reason and server timestamp are recorded in history/audit.

### `POST /api/v1/officer/applications/{id}/reject/`

**Purpose:** Reject an application with an explicit human-readable reason and immutable history/audit entries.

**Permission:** Same-authority `LOCAL_OFFICER`.

**Request**

```json
{
  "reason": "ข้อมูลอาคารไม่อยู่ในขอบเขตที่หน่วยงานสามารถพิจารณาได้"
}
```

**Success**

```json
{
  "id": 101,
  "status": "REJECTED",
  "reason": "ข้อมูลอาคารไม่อยู่ในขอบเขตที่หน่วยงานสามารถพิจารณาได้",
  "changed_at": "2026-09-19T08:00:00Z"
}
```

**Important errors:** `400 REASON_REQUIRED`; `404 NOT_FOUND`; `409 INVALID_STATUS_TRANSITION`; `409 LICENSE_ALREADY_ISSUED`. An approved/licensed application cannot be rejected through this endpoint.

### `POST /api/v1/officer/applications/{id}/central-assistance/`

**Purpose:** Ask the central office for structured guidance without sharing an individual application.

**Permission:** Verified `LOCAL_OFFICER` assigned to the application's authority. The application must be `SUBMITTED`, `UNDER_REVIEW`, or `RESUBMITTED`, and must not already have an open request.

**Body:** `{ "question_code": "CLASSIFICATION_AMBIGUITY|REQUIREMENT_APPLICABILITY|WORKFLOW_EXCEPTION|POLICY_INTERPRETATION" }`

**Success:** `201` with an opaque UUID `reference`, status, controlled question, structured classification/capacity/workflow/checklist snapshot, nullable controlled resolution, and timestamps. The response never includes an application ID/reference, applicant, property name/address/contact, file, officer, or authority identity.

**Errors:** `404 NOT_FOUND`, `409 ASSISTANCE_NOT_AVAILABLE`, or `409 ASSISTANCE_ALREADY_OPEN`.

### `GET|POST /api/v1/applications/{id}/discussion/`

**Permission:** Owning verified applicant. `GET` returns immutable messages oldest first. `POST` accepts `{ "body": "..." }` up to 2,000 characters only while the submitted application is active.

### `GET|POST /api/v1/officer/applications/{id}/discussion/`

**Permission:** Verified local officer assigned to the application's authority. Behavior matches the applicant endpoint. Each message returns only `id`, `sender_category`, `body`, and server `created_at`; it cannot be updated or deleted.

---

## Central overview

### `GET /api/v1/central/assistance/`

**Purpose:** List up to 100 privacy-safe guidance records newest first. Optional `?status=OPEN|RESOLVED` filter.

**Permission:** `CENTRAL_OFFICER` only.

**Success:** `{ "results": [...] }` using the same PII-free projection. This separate queue does not grant access to the linked application.

### `POST /api/v1/central/assistance/{reference}/resolve/`

**Purpose:** Record structured central guidance without changing the application.

**Permission:** `CENTRAL_OFFICER` only.

**Body:** `{ "resolution_code": "FOLLOW_CURRENT_RULES|REQUEST_MORE_EVIDENCE|ESCALATE_OFFLINE|NO_CENTRAL_DECISION" }`

**Success:** `200` with the resolved privacy-safe record. A second resolution returns `409 ASSISTANCE_ALREADY_RESOLVED`. Resolution creates an immutable audit event but no application status transition.

### `GET /api/v1/central/summary/`

**Purpose:** Return province-wide aggregate counts without individual application records or decision controls.

**Permission:** `CENTRAL_OFFICER` only. `LOCAL_OFFICER` cannot use this endpoint, and central officers cannot use individual officer/applicant detail endpoints.

**Request example:** `GET /api/v1/central/summary/`

**Success**

```json
{
  "generated_at": "2026-09-19T08:05:00Z",
  "totals": {
    "applications": 12,
    "waiting_review": 4,
    "waiting_for_applicant_revision": 2,
    "approved": 5
  },
  "by_property_type": [
    { "code": "TYPE_1", "name": "ที่พักแรมประเภทที่ 1", "count": 8 },
    { "code": "TYPE_2", "name": "ที่พักแรมประเภทที่ 2", "count": 4 }
  ],
  "by_local_authority": [
    {
      "id": 1,
      "code": "PHUKET_PAO",
      "name": "องค์การบริหารส่วนจังหวัดภูเก็ต",
      "count": 2,
      "totals": {
        "applications": 2,
        "waiting_review": 0,
        "waiting_for_applicant_revision": 0,
        "approved": 0
      },
      "by_property_type": [
        { "code": "TYPE_1", "name": "ที่พักแรมประเภทที่ 1", "count": 1 },
        { "code": "TYPE_2", "name": "ที่พักแรมประเภทที่ 2", "count": 1 }
      ],
      "by_current_stage": [
        { "stage": "APPLICANT_PREPARATION", "count": 2 }
      ]
    }
  ],
  "authority_count": 19,
  "expected_authority_count": 19,
  "authority_zeroes_included": true,
  "by_current_stage": [
    { "stage": "LOCAL_OFFICER_REVIEW", "count": 4 },
    { "stage": "APPLICANT_ACTION", "count": 2 },
    { "stage": "COMPLETED", "count": 6 }
  ]
}
```

`waiting_review` is the documented grouping of `SUBMITTED`, `UNDER_REVIEW`, and `RESUBMITTED`; `waiting_for_applicant_revision` maps to `REVISION_REQUIRED`; `approved` maps to `APPROVED`. `applications` includes all application statuses unless the contract is deliberately revised.

`by_local_authority` contains every active configured LocalAuthority, including zero-count rows, so an overview never confuses an omitted area with zero. Each row includes aggregate-only totals and type/stage breakdowns for an inline area-detail panel; it never contains applicant, property-address, document, reason, or individual application identifiers. The array is master-data order; clients may additionally rank it for a table or heat grid.

`timing_analytics.average_wait_by_stage` contains `stage`, `average_hours`, and `sample_count` derived from status-history intervals. `timing_analytics.overdue` contains the configured `threshold_days`, aggregate `total`, and `by_stage` counts for non-terminal applications whose current stage age meets the threshold. It contains no individual case or officer data.

`anonymous_workload` contains calendar `period`, `minimum_group_size`, nullable `contributor_count`, `suppressed`, and rows of rotating `officer_reference`, `document_reviews`, `application_decisions`, and `total_actions`. When contributors are below the privacy threshold, `contributor_count` is null and `rows` is empty. It never returns user ID, name, email, or authority.

**Important errors:** `401 AUTHENTICATION_REQUIRED`; `403 PERMISSION_DENIED`. Empty application data returns zero totals, empty type/stage groups, and all active authority rows with zero counts rather than `404`.

---

## Printable decision artifact

### `GET /api/v1/applications/{id}/license/`

**Purpose:** Return the approved hotel licence or non-hotel notification acknowledgement used by the applicant's print-friendly HTML page.

**Permission:** Owning `APPLICANT`; a same-authority `LOCAL_OFFICER` may read it for operational verification. `CENTRAL_OFFICER` has no individual access. Super admins use Django Admin.

**Request example:** `GET /api/v1/applications/101/license/`

**Success**

```json
{
  "id": 501,
  "artifact_kind": "HOTEL_LICENSE",
  "license_number": "LIC-2026-00001",
  "application_reference_number": "HTL-2026-00001",
  "property": {
    "name": "ที่พักตัวอย่างอันดามัน",
    "address": "99 ถนนตัวอย่าง ป่าตอง กะทู้ ภูเก็ต 83150"
  },
  "property_type": {
    "code": "TYPE_1",
    "name": "ที่พักแรมประเภทที่ 1"
  },
  "issuing_authority": {
    "id": 4,
    "code": "PATONG_MUNICIPALITY",
    "name": "เทศบาลเมืองป่าตอง"
  },
  "issued_at": "2026-09-19T08:00:00Z",
  "expires_at": "2031-09-18",
  "fee": {
    "amount_snapshot": "10000.00",
    "currency": "THB",
    "fee_schedule_id": 11
  },
  "public_verification": {
    "verification_url": "http://localhost:5173/verify/11111111-1111-4111-8111-111111111111",
    "qr_code_url": "/api/v1/public/licenses/11111111-1111-4111-8111-111111111111/qr/"
  }
}
```

**Important errors:** `404 NOT_FOUND` for missing/out-of-scope application; `409 LICENSE_NOT_AVAILABLE` if the application is not approved or license creation has not completed. The endpoint returns data, not a sophisticated generated PDF; the Vue route renders accessible print-friendly HTML/CSS.

For `NOTIFICATION_ACKNOWLEDGEMENT`, `expires_at` and `fee` are null and the printable UI uses acknowledgement wording rather than licence wording.

### `GET /api/v1/public/licenses/{verification_token}/`

**Purpose:** Let any visitor verify a printed decision artifact from an opaque QR/link without signing in.

**Permission:** Public, anonymous-throttled. Lookup is by immutable random UUID, not by the sequential licence number.

**Success:** Returns only `status` (`VALID`, `EXPIRED`, or `RECORDED`), `artifact_kind`, `license_number`, property display name/type, issuing authority, issue/expiry dates, and `checked_at`. It never returns applicant identity, application reference, exact address, fee, document metadata, reasons, or audit history.

**Important errors:** `404 NOT_FOUND` for an unknown or malformed token.

### `GET /api/v1/public/licenses/{verification_token}/qr/`

**Purpose:** Return the server-generated SVG QR code placed on the printable artifact. The encoded value is exactly `{FRONTEND_BASE_URL}/verify/{verification_token}`; clients cannot supply arbitrary QR content.

**Permission:** Public, anonymous-throttled. **Response content type:** `image/svg+xml`. **Important errors:** `404 NOT_FOUND`.

## Contract invariants

1. Clients never choose an application status, reference number, audit timestamp, document version, fee snapshot, or license number.
2. Every workflow transition is validated centrally and creates status history plus an immutable audit record in one database transaction.
3. Applicant ownership and local-authority scope apply to querysets, detail lookups, nested resources, and file downloads.
4. Central access to applications is aggregate-only; the separate S5B queue exposes and mutates only controlled guidance records with no individual identifier or free text.
5. Current database rules and requirements are authoritative; client-cached classification/checklist data is advisory.
6. Demo checklist and contact content must remain visibly marked as mock/unverified until officially validated.
